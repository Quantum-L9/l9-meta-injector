# Local-only credentials for manual smoke testing. Never commit the real values.
# Copy this file to .env and fill in the real key, or use .env directly (both are gitignored).

# OpenAI API key used by the org's l9-llm-selftest workflow (gpt-5-nano).
# Same secret as the GitHub org secret OPENAI_API_KEY.
OPENAI_API_KEY=
