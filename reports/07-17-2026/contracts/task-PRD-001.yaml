# --- Flawless-Victory task derived from finding PRD-001 ---
task:
  id: "PRD-001"
  objective: "Implement near-duplicate detection using hashPrefix/threshold and populate nearDuplicates, or remove nearDupThreshold from the config surface and document exact-twin-only."
  scope_fence:
    owner_layer: "src/compiler.ts"
    allowed_paths:
      - "src/compiler.ts"
      - "schema.ts"
      - "pipeline.ts"
  inspect_first:
    - "src/compiler.ts:17"
    - "schema.ts:155"
    - "pipeline.ts:93"
  constraint_to_satisfy: "Pass 2 stub detection: advertised feature is a placeholder returning a hardcoded empty value | CWE=CWE-1164 | category=stub"
  base_ref: "HEAD@3fba77d"
  regression_target: "Near-duplicate detection is silently a no-op; users configuring the threshold get false assurance while near-duplicate artifacts pass undetected."
  severity: "medium"
  blocks_release: false
  stop_if:
    - "fix requires editing paths outside allowed_paths"
    - "fix would weaken constraint_to_satisfy"
    - "fix requires out-of-scope work"
    - "validation cannot be run honestly"
  followup_verify:
    audit: "05-followup-remediation-audit.md"
    require_verified_status: "resolved"
    before_advancing_to_next_finding: true
