# --- Flawless-Victory task derived from finding DWL-006 ---
task:
  id: "DWL-006"
  objective: "Remove promptGlob from NamespaceConfig (and the hardcoded value in toCfg), or consume it where prompt globbing is decided."
  scope_fence:
    owner_layer: "src/namespace.ts"
    allowed_paths:
      - "src/namespace.ts"
      - "pipeline.ts"
      - "namespace.ts"
      - "cfg.promptGlob"
      - "cfg.namespace"
      - "cfg.namespaceGlobs"
  inspect_first:
    - "src/namespace.ts:20"
    - "pipeline.ts:19"
    - "namespace.ts"
    - "cfg.promptGlob"
    - "cfg.namespace"
    - "cfg.namespaceGlobs"
  constraint_to_satisfy: "orphaned_config_schema (set but never read) | category=orphaned_config_schema"
  base_ref: "HEAD@3fba77d"
  regression_target: "A required config field is manufactured on every call but never consumed, implying prompt-glob configurability that doesn't exist and forcing all callers/tests to supply a dead value."
  severity: "low"
  blocks_release: false
  stop_if:
    - "fix requires editing paths outside allowed_paths"
    - "fix would weaken constraint_to_satisfy"
    - "fix requires out-of-scope work"
    - "validation cannot be run honestly"
  followup_verify:
    audit: "05-followup-remediation-audit.md"
    require_verified_status: "resolved"
    before_advancing_to_next_finding: true
