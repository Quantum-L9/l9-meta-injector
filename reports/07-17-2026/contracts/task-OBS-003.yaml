# --- Flawless-Victory task derived from finding OBS-003 ---
task:
  id: "OBS-003"
  objective: "Return/log a coverage tally (scanned, injected, skippedBinary, skippedNonInjectable, verifyFailed) with skipped source paths."
  scope_fence:
    owner_layer: "src/pipeline.ts"
    allowed_paths:
      - "src/pipeline.ts"
      - "spec.strategy"
      - "meta.injectable"
  inspect_first:
    - "src/pipeline.ts:49"
    - "spec.strategy"
    - "src/pipeline.ts:83"
    - "meta.injectable"
  constraint_to_satisfy: "pipeline-must-report-what-it-did-and-skipped | CWE=CWE-778 | category=missing-diagnostics"
  base_ref: "HEAD@3fba77d"
  regression_target: "Library path produces zero operator-visible coverage signal (scanned vs injected vs skipped vs verify-failed); silent coverage holes."
  severity: "high"
  blocks_release: false
  stop_if:
    - "fix requires editing paths outside allowed_paths"
    - "fix would weaken constraint_to_satisfy"
    - "fix requires out-of-scope work"
    - "validation cannot be run honestly"
  followup_verify:
    audit: "05-followup-remediation-audit.md"
    require_verified_status: "resolved"
    before_advancing_to_next_finding: true
