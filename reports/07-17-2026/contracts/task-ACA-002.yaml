# --- Flawless-Victory task derived from finding ACA-002 ---
task:
  id: "ACA-002"
  objective: "Add/replace architecture.md to document the TS pipeline (retrieval→extract→classify→normalize_meta→inject→verify→compiler) as primary; scope the Python tool as secondary."
  scope_fence:
    owner_layer: "docs/architecture.md"
    allowed_paths:
      - "docs/architecture.md"
      - "ingress.py"
      - "/injector.py"
      - "package.json"
      - "dist/index.js"
      - "src/pipeline.ts"
  inspect_first:
    - "docs/architecture.md:5"
    - "ingress.py"
    - "/injector.py"
    - "package.json"
    - "dist/index.js"
    - "src/pipeline.ts"
  constraint_to_satisfy: "Pass 1 context lock — as-designed doc must describe the as-built primary artifact | category=documentation-drift"
  base_ref: "HEAD@3fba77d"
  regression_target: "The authoritative architecture reference doesn't cover the shipped artifact; conformance can't be checked against a spec and contributors design against the wrong model."
  severity: "medium"
  blocks_release: false
  stop_if:
    - "fix requires editing paths outside allowed_paths"
    - "fix would weaken constraint_to_satisfy"
    - "fix requires out-of-scope work"
    - "validation cannot be run honestly"
  followup_verify:
    audit: "05-followup-remediation-audit.md"
    require_verified_status: "resolved"
    before_advancing_to_next_finding: true
