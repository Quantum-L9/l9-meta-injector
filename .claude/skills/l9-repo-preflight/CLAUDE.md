# Claude Code Use

Run `python scripts/run_preflight.py <repo> --artifact-dir artifacts/preflight`.

Use configured MCP servers for remote repository and PR evidence. Advertise supported operations instead of assuming that MCP availability grants Python subprocess access. The active Claude session may satisfy Gate 20 host reasoning through the structured request artifact; no external LLM key is required. Preserve deterministic results when reasoning is unavailable.
