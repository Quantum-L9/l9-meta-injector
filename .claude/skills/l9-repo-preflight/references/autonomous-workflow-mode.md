# Autonomous Workflow Mode

Autonomous Git workflows default to `auto`, do not require reasoning, preserve static results, and emit a reasoning request artifact when no provider is callable. A later interactive agent may return a validated response bound to the same evidence digest. Release policy may explicitly require enriched reasoning, but ordinary preflight does not.
