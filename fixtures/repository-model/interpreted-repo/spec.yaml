service:
  name: "example-service"
  version: "0.2.0"
  description: "Spec-driven engine surface"

actions:
  - name: "execute"
    description: "Execute an action request"
    input_schema:
      type: object
      properties:
        action_name:
          type: string
      required: ["action_name"]

  - name: "describe"
    description: "Describe engine capabilities"
    input_schema:
      type: object
      properties: {}
      required: []
